import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    input__container: {
        fontSize: 20,
        color: 'black',
        fontWeight: 'bold'
    },
    input__TextField: {
        borderWidth: 2,
        borderColor: 'gray',
        padding: 10,
        borderRadius: 5,
        fontSize: 18,
        marginTop: 10
    },
    input__ErrorTextField: {
        borderWidth: 2,
        borderColor: 'red',
        padding: 10,
        borderRadius: 5,
        fontSize: 18,
        marginTop: 10
    }
})
