import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    step1__container: {
        textAlign: 'center',
        fontSize: 20,
        fontWeight: 'bold',
        marginVertical: 10
    }
})
